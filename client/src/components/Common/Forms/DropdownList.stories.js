import DropdownList from './DropdownList';

export default {
  title: 'Building Blocks / DropdownList',
  component: DropdownList,
};

const Template = (args) => <DropdownList />;

export const Default = Template.bind({});
